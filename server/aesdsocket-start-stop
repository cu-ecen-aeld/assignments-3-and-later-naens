#!/bin/sh

if [ $# = 1 ]
then
    server="aesdsocket"
    if [ "$1" = "start" ]
    then
        start-stop-daemon -v -S -n $server
    elif [ "$1" = "stop" ]
    then
        start-stop-daemon -v -K -n $server
    fi
fi
